
@model IEnumerable<EventsUs.Models.ApplicationUser>
@{
    ViewData["Title"] = "Index";
}
<img src="https://image.ibb.co/k3keCz/log.png">

<style type="text/css">
    .border {
    }

    .table-info {
    }

    .col-6 {
    }
</style>

<bar /><bar />


<div Class="row ">
    <div class="col-6">
        <h2 class="text-info">Users List</h2>
    </div>
    <div class="col-6 text-right">
    </div>
</div>

@using (Html.BeginForm())
{
    <p>
        Find by Id: @Html.TextBox("SearchString1")
        Age: @Html.TextBox("SearchString2")
        Country: @Html.TextBox("SearchString3")
        <input type="submit" value="Search" />
    </p>
}

<br />
<div>
    <table class="table table-striped border">
        <tr class="table-info">
            <th>
                @Html.DisplayNameFor(m => m.Name)
            </th>
            <th>
                @Html.DisplayNameFor(m => m.Email)
            </th>
            <th>
                @Html.DisplayNameFor(m => m.PhoneNumber)
            </th>
            <th>
                @Html.DisplayNameFor(m => m.Age)

            </th>
            <th>
                @Html.DisplayNameFor(m => m.Country)

            </th>
            <th> </th>

            @foreach (var item in Model)
            {
            <tr>
                <td>
                    @Html.DisplayFor(m => item.Name)
                </td>
                <td>
                    @Html.DisplayFor(m => item.Email)
                </td>
                <td>
                    @Html.DisplayFor(m => item.PhoneNumber)
                </td>
                <td>
                    @Html.DisplayFor(m => item.Age)
                </td>
                <td>
                    @Html.DisplayFor(m => item.Country)
                </td>
                <td>
                    @if (item.LockoutEnd != null && item.LockoutEnd > DateTime.Now)
                    {
                        <label>Disabled</label>
                    }
                </td>


            </tr>
        }
        </table>

    </div>


